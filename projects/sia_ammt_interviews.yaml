version: 1.0.0
project:
  id: "sia_ammt_interviews"
  name: "SIA - AMMT/Interviews"
  category: "work_internal"
  domain: "career_development"
  sensitivity: "internal"

purpose:
  description: >
    Work on the SIA - AMMT/Interviews project with durable notes, decisions, and artifacts.

scope:
  included:
    - "troubleshooting"
    - "RCA"
    - "SOPs"
    - "training plans"
    - "dashboards"
    - "SharePoint/Power Platform"
  excluded:
    - "speculative blame"
    - "unsafe procedures"
    - "policy guesses without sources"

default_roles:
  primary: "research"
  secondary:
    - "research"
    - "trainer_cap"

interaction_overrides:
  tone: "safety-first, precise"
  assumptions:
    - "state assumptions explicitly"
    - "separate facts from inference"

outputs:
  preferred:
    - "tables"
    - "decision gates (if X then Y)"
    - "stepwise SOPs"
    - "checklists"
  required_sections:
    - "Answer"
    - "Top hypotheses / options"
    - "Tests / steps (ordered)"
    - "Stop conditions / safety notes"
    - "Next actions"

handoff:
  store_results_in:
    - "GitHub (ai-context repo)"
    - "Notion"
    - "SharePoint (when applicable)"
