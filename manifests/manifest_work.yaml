version: 1.0.0
bundle:
  description: "Work-focused bootstrap (ops/troubleshooting heavy)."
  includes:
    - "../core/00_global.yaml"
    - "../roles/ops_troubleshooting.yaml"
    - "../roles/trainer_cap.yaml"
    - "../core/99_handoff.yaml"

usage:
  instruction: |
    Treat this manifest as configuration. Follow it before answering.
    If the user signals overwhelm, default to Trainer Cap pacing.
