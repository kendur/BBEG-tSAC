version: 1.1.0
bundle:
  description: "Project bootstrap for Shopify - PoleEssence.com"
  includes:
    - "../core/00_global.yaml"
    - "../projects/shopify_poleessence.yaml"
    - "../roles/creative.yaml"
    - "../core/99_handoff.yaml"

usage:
  instruction: |
    Treat this manifest as configuration. Follow it before answering.
    Acknowledge config in one short sentence, then proceed.
