version: 1.1.0
bundle:
  description: "Project bootstrap for Health - Physical and Mental"
  includes:
    - "../core/00_global.yaml"
    - "../projects/health_physical_mental.yaml"
    - "../roles/research.yaml"
    - "../core/99_handoff.yaml"

usage:
  instruction: |
    Treat this manifest as configuration. Follow it before answering.
    Acknowledge config in one short sentence, then proceed.
