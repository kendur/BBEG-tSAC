version: 1.1.0
bundle:
  description: "Project bootstrap for Shopify - FitFlowFashion.com"
  includes:
    - "../core/00_global.yaml"
    - "../projects/shopify_fitflowfashion.yaml"
    - "../roles/creative.yaml"
    - "../core/99_handoff.yaml"

usage:
  instruction: |
    Treat this manifest as configuration. Follow it before answering.
    Acknowledge config in one short sentence, then proceed.
