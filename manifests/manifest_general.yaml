version: 1.0.0
bundle:
  description: "General-purpose bootstrap for stateless models."
  includes:
    - "../core/00_global.yaml"
    - "../roles/research.yaml"
    - "../core/99_handoff.yaml"

usage:
  instruction: |
    Treat this manifest as configuration. Follow it before answering.
    Acknowledge config in one short sentence, then proceed.
